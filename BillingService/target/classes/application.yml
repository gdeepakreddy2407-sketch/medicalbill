spring:
  application:
    name: BillingService

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/catalog_db}

server:
  port: ${PORT:8080}
eureka:
  client:
    enabled: false
logging:
  level:
    root: INFO
